import { mockVenues } from "../data/mockExploreVenues"

export const fetchVenues = async () => {
  // Future API integration: replace this return with an axios call to the venues endpoint.
  return mockVenues
}
